import { ValidaCepDirective } from './valida-cep.directive';

describe('ValidaCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaCepDirective();
    expect(directive).toBeTruthy();
  });
});
